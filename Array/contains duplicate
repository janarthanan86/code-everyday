class Solution {
    public boolean hasDuplicate(int[] nums) {
    HashSet<Integer>seen=new HashSet<>();
    for(int a:nums)
    {
        if(seen.contains(a))
        return true;
        seen.add(a);
    }    
    return false;
    }
}
